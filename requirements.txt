# /wissenschaft — Laufzeit-Abhängigkeiten
# Versionen mit Unter- UND Obergrenze: reproduzierbar, aber updatebar.
# Getestet mit den unten angegebenen Versionen (CI: Python 3.10/3.11/3.12).
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Multi-Quellen-Connectors (vendor/paper_search_mcp)
requests>=2.34,<3
feedparser>=6.0,<7
beautifulsoup4>=4.15,<5
pypdf>=6.18,<7

# Registry-Routing (source_router.py)
pyyaml>=6.0,<7

# Tests
pytest>=9.1,<10

# MCP-Modus (Option B): Server mcp_server.py
mcp[cli]<2
fastmcp>=3.4,<4
